function itung(numbers) {
    let counter=0
    for (let i = 0; i < numbers.length; i++) {
      return  i
        return `${counter}`
        i++
    
        
        
    }
}

let numbers=[144,253,3,44,5,6];